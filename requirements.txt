# Install with:
#   python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.4.1 torchaudio==2.4.1
#   python -m pip install -r requirements.txt -c constraints-win-cpu.txt
#
# The constraints file intentionally pins the torch/torchaudio/pyannote/numpy
# stack for Windows CPU compatibility.

numpy
scipy>=1.10.0
soundfile>=0.12.0
pyannote.audio
speechbrain
openai-whisper
faiss-cpu
scikit-learn
pytest>=7.0.0
pytest-cov>=4.0.0
python-dotenv>=1.0.0
transformers>=4.30.0,<5.0
sentencepiece
safetensors>=0.4.0
modelscope>=1.10.0
