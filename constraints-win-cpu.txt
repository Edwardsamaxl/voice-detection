# Windows CPU compatibility lock for Python 3.11.
#
# Install torch packages from the PyTorch CPU wheel index first:
#   python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.4.1 torchaudio==2.4.1
# Then install the project dependencies with this constraints file:
#   python -m pip install -r requirements.txt -c constraints-win-cpu.txt

torch==2.4.1
torchaudio==2.4.1
numpy==1.26.4
pyannote.audio==3.3.2
pyannote.core<6
pyannote.metrics<4
speechbrain==1.1.0
scikit-learn>=1.3.0,<1.8
faiss-cpu>=1.8.0
openai-whisper>=20231117
